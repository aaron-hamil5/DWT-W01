console.log("Welcome to the first lab of Dynamic Web Technologies");

// Create a simple script that uses variables to store a user's first name and last name. Then, use string
// concatenation or template literals to combine these variables into a full name and print it to the console

const firstName = prompt("What is your first name?");
const lastName = prompt("What is your last name?");
const fullName = firstName + " " + lastName;
console.log(fullName);
